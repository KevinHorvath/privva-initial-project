<div class="container">
  <%- flash.each do |name, msg| %>
    <div class="<%= if name == 'alert' then 'alert-danger' elsif name == 'notice' then 'alert-success' end %> alert alert-dismissible fade show" role="alert">
      <button aria-label="Close" class="close" data-dismiss="alert" type="button">
        <span aria-hidden="true">×</span>
      </button>
      <%= msg %>
    </div>
  <% end %>
</div>
